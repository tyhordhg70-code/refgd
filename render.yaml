services:
  - type: web
    name: refgd
    rootDir: refgd
    buildCommand: corepack enable && pnpm install --frozen-lockfile --ignore-workspace && pnpm run build
    startCommand: pnpm start
    env: node
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
