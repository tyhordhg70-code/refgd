import type { Metadata } from "next";
import PageContent from "./page-content";

export const metadata: Metadata = {
  title: "Evade Cancelations — RefundGod",
  description:
    "Master the art of navigating store policies and algorithms. Learn the techniques to place orders with confidence and succeed where others fail.",
};

export default function EvadePage() {
  return <PageContent />;
}
