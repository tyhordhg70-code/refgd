# Test Credentials

## Admin
- URL: `/admin`
- Username: `admin` (override via `ADMIN_USERNAME` env var)
- Password: `kaizokuoniwa`

After login → redirects to `/admin/dashboard`. Inline editor toolbar appears at bottom of every page when admin session is active.
